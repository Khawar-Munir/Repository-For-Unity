using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GitTesrt : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //*123

        //* change made through vs
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
